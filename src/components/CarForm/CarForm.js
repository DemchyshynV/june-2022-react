import {useForm} from "react-hook-form";

const CarForm = () => {
    const {handleSubmit, register} = useForm();

    return (
        <div>
            CarForm
        </div>
    );
};

export {CarForm};
